{
  "buildCommand": "npm run build && npx esbuild api/index.ts --bundle --platform=node --format=cjs --packages=external --outfile=api/dist.cjs",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/dist.cjs"
    },
    {
      "source": "/((?!api/.*).*)",
      "destination": "/index.html"
    }
  ]
}
